export const generateSummary = (data: any) => {
  // Generate a plain-English summary based on the data
  return 'Your watchlist is up 2.3% today. AAPL drove most gains.';
};

export const generateExplainer = (topic: string) => {
  // Generate an explainer for the given topic
  return 'This is an explanation of the topic.';
};
