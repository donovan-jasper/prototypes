const queryBuilder = (query) => {
  // Parse SQL-like query
  const parsedQuery = { where: { sentiment: 'negative' } };

  // Execute query against SQLite
  // Return results
};

export default queryBuilder;
