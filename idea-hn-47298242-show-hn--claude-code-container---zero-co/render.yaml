services:
  - type: web
    name: codecapsule-backend
    env: docker
    dockerfilePath: ./Dockerfile
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
    scaling:
      minInstances: 1
      maxInstances: 3
