{
  "firestore": {
    "rules": "rules_version = '2';\n\nservice cloud.firestore {\n  match /databases/{database}/documents {\n    match /submissions/{submissionId} {\n      allow read, write: if request.auth != null;\n    }\n    match /feedback/{feedbackId} {\n      allow read, write: if request.auth != null;\n    }\n    match /sprintQueue/{queueId} {\n      allow read, write: if request.auth != null;\n    }\n  }\n}"
  }
}
